# 01 · 터미널 안 무서워하기

`[손]` — 직접 손으로 해봐야 익혀집니다.

> 터미널이 "컴퓨터와 대화하는 창"임을 이해하고, 겁먹지 않을 생존 동작 4~5개만 익힙니다.

---

> 🪟 **Windows 사용자** — 이 페이지를 시작하기 전에 [setup-windows](setup-windows.md)를 먼저 끝내세요.  
> 셸에 들어오는 방법: WSL 사용자는 **Ubuntu** 앱을 여세요. Mac 사용자는 **터미널** 앱을 여세요.  
> 프롬프트(`$` 또는 `%`)가 보이면 준비 완료입니다. 이후 명령어는 **모두 동일**합니다.

---

## 개념

컴퓨터를 조작하는 방법은 두 가지입니다.

- **GUI** — 마우스로 클릭하는 방식. Finder(Mac)나 파일 탐색기(Windows)가 여기에 해당합니다.
- **CLI** — 키보드로 명령어를 입력하는 방식. 터미널이 바로 이것입니다.

터미널이 낯선 이유는 클릭할 버튼이 없기 때문입니다. 하지만 우리가 배울 명령어는 딱 다섯 개입니다.

---

### 프롬프트란

셸을 열면 이런 게 보입니다.

```
사용자명@컴퓨터이름:~$
```

맨 끝의 `$`가 **프롬프트**입니다. "입력할 준비가 됐어요"라는 신호입니다. 여기에 명령어를 입력하고 Enter를 누르면 컴퓨터가 실행하고 결과를 출력합니다.

---

### 생존 명령어 5개

| 명령어 | 하는 일 | 외우는 법 |
| --- | --- | --- |
| `pwd` | 내가 지금 어느 폴더에 있는지 확인 | **P**rint **W**orking **D**irectory |
| `ls` | 이 폴더 안에 뭐가 있는지 목록 출력 | **L**i**s**t |
| `cd 폴더이름` | 그 폴더 안으로 이동 | **C**hange **D**irectory |
| `↑` (위쪽 화살표) | 방금 입력한 명령어 다시 불러오기 | — |
| `Ctrl + C` | 실행 중인 것을 멈추고 탈출 | — |

이 다섯 개면 오늘 실습을 모두 진행할 수 있습니다.

---

## 쉬운 비유

터미널은 **카운터 주문**과 같습니다.

- GUI는 그림 메뉴판을 손가락으로 가리키는 것입니다.
- CLI는 "아메리카노 한 잔 주세요"라고 말로 주문하는 것입니다.

말로 하면 더 빠르고 정확합니다. 단, 메뉴 이름을 정확히 알아야 합니다. 터미널도 마찬가지입니다. 명령어 철자가 틀리면 컴퓨터가 못 알아듣습니다.

---

## 해보기

아래 순서대로 직접 입력해 보세요. 명령어를 입력하고 Enter를 누릅니다.

**1. 내 위치 확인**

```bash
pwd
```

`/home/사용자명` 또는 `/Users/사용자명` 같은 경로가 출력됩니다. 지금 내가 있는 폴더입니다.

---

**2. 폴더 안 목록 보기**

```bash
ls
```

현재 폴더에 있는 파일과 폴더 이름이 나열됩니다.

---

**3. 폴더 이동**

```bash
cd ~
```

`~`는 홈 폴더를 뜻합니다. 어디서든 이 명령어를 입력하면 홈으로 돌아옵니다.

```bash
pwd
```

다시 위치를 확인해 보세요. 홈 폴더로 이동했을 것입니다.

---

**4. 위쪽 화살표로 재사용**

`↑` 키를 눌러 보세요. 방금 입력한 `pwd`가 다시 나타납니다. Enter를 누르면 재실행됩니다. 긴 명령어를 다시 타이핑하지 않아도 됩니다.

---

**5. Ctrl+C로 탈출**

```bash
cat
```

입력하고 Enter를 누르면 커서가 멈추고 아무것도 안 됩니다. 당황하지 마세요. `Ctrl + C`를 누르면 즉시 빠져나옵니다.

---

## 흔한 실수

**대소문자가 구별됩니다.**

`ls`와 `LS`는 다릅니다. `LS`를 입력하면 `command not found` 오류가 납니다. 명령어는 소문자로 입력하세요.

---

**오류 메시지가 나도 컴퓨터가 망가진 게 아닙니다.**

```
bash: lls: command not found
```

이런 메시지는 "그런 명령어 모르겠어요"라는 뜻입니다. 철자를 확인하고 다시 입력하면 됩니다.

---

**뭔가 잘못됐다 싶으면 Ctrl+C.**

명령어가 멈춰 있거나 이상하게 돌아갈 때 `Ctrl + C`를 누르면 대부분 해결됩니다. 터미널의 비상구입니다.

---

**경로에 띄어쓰기가 있으면 따옴표로 감쌉니다.**

```bash
cd "내 폴더"
```

띄어쓰기가 없는 폴더 이름을 쓰는 게 가장 편합니다.
