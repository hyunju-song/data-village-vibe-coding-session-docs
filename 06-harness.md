# 06 · 하네스: 왜 & 어떻게

`[읽기]` — 개념만 읽고 넘기면 됩니다. 외울 필요 없습니다.

> 앞에서 겪은 반복·위험을 하네스가 어떻게 없애주는지, "가볍게 만든 걸 하네스 틀에 맞춰 리팩토링"이 뭔지.

---

> 📎 **회사 노션 상세 자료** — 이 장의 심화 내용과 보충 설명은 노션에 있습니다.  
> 링크: <!-- NOTION_LINK -->

---

## 개념

### 하네스가 없으면 어떤 일이 생기나

대시보드를 하나 만들 때마다 아래 작업을 처음부터 반복해야 합니다.

- Amplify에 레포 연결
- SSO(로그인) 설정
- 환경변수(API 키, DB 주소 등) 관리
- Lambda 함수 배포 설정
- 훅 ✅ 점검 스크립트 작성

이걸 매번 직접 하면 설정에만 반나절이 걸리고, 한 군데 실수하면 보안 사고가 날 수 있습니다. 그리고 사람마다 설정 방식이 달라지면 팀이 서로 도울 수 없습니다.

---

### 하네스란

**하네스(harness)** 는 이 반복 작업을 미리 다 해둔 **틀**입니다.

레포를 만들 때 하네스 템플릿을 사용하면, 배포·보안·SSO·훅이 이미 연결된 상태로 시작합니다. 우리가 할 일은 **그 틀 안에서 아이디어(코드)만 채우는 것**입니다.

```
하네스 템플릿 (이미 세팅된 것들)
┌─────────────────────────────────────┐
│  Amplify 연결     ✅                 │
│  SSO 설정         ✅                 │
│  환경변수 관리    ✅                 │
│  Lambda 배포      ✅                 │
│  훅 점검 스크립트 ✅                 │
│                                     │
│  [ 여기에 내 코드만 채운다 ]         │
└─────────────────────────────────────┘
```

---

### 리팩토링이란

처음에는 아이디어가 빨리 나오도록 **가볍게** 코드를 짭니다. Claude Code와 대화하며 "이런 화면 만들어줘", "이 데이터 꺼내줘"를 빠르게 시도합니다.

그런데 이 상태로는 하네스가 인식하지 못하는 구조일 수 있습니다. 파일 위치, 함수 이름, 설정 방식이 하네스의 기대와 다를 때입니다.

**리팩토링**은 이 코드를 하네스가 이해하는 구조로 다시 정리하는 과정입니다. 결과는 같지만 구조가 더 나아집니다.

```
가볍게 만든 것 (아이디어 검증)
    │
    ▼ 리팩토링 (Claude Code가 대부분 처리)
하네스 틀에 맞는 구조 (배포·보안 자동)
    │
    ▼
main merge → 자동 배포 → 훅 ✅
```

---

### Claude Code가 리팩토링을 대신하는 방식

리팩토링은 우리가 직접 다 할 필요가 없습니다. Claude Code에 아래처럼 말하면 됩니다.

> "이 코드를 하네스 구조에 맞게 리팩토링해줘"

Claude Code가 파일 위치를 옮기고, 함수 이름을 맞추고, 설정 파일을 정리합니다. 우리는 결과가 맞는지만 확인하면 됩니다.

오전 실습에서 이 과정을 한 번 같이 해봅니다. 오후 부트캠프에서는 혼자 해봅니다.

---

## 쉬운 비유

하네스는 **레고 베이스플레이트**와 같습니다.

베이스플레이트 없이 레고를 쌓으면 금방 무너집니다. 베이스플레이트가 있으면 어디에 블록을 올려도 단단하게 붙습니다.

| 레고 | 하네스 |
| --- | --- |
| 베이스플레이트 | 하네스 (이미 세팅된 틀) |
| 내가 만드는 작품 | 내가 짜는 코드 (아이디어) |
| 블록이 맞는 규격 | 하네스가 기대하는 파일 구조·함수 이름 |
| 베이스 위에 올리기 | 리팩토링 |

베이스플레이트를 직접 만들 필요는 없습니다. 블록만 올리면 됩니다.

---

## 해보기

이 장은 읽기 장입니다. 실제 하네스 사용은 **오후 부트캠프**에서 합니다.

아래 질문에 답해 보세요.

- 하네스 없이 대시보드를 처음부터 만들면 어떤 작업들을 직접 해야 하나요?
- 리팩토링 전과 후의 차이는 무엇인가요? (기능 vs 구조)
- Claude Code에 리팩토링을 맡길 때 우리가 직접 해야 하는 건 무엇인가요?

---

## 흔한 실수

**하네스 밖에서 파일을 만들기**

하네스는 특정 폴더 구조를 기대합니다. 파일을 임의의 위치에 만들면 배포 스크립트가 그 파일을 찾지 못합니다. 결과적으로 훅 ✅이 안 뜨거나 배포가 실패합니다.

파일을 어디에 만들지 모르겠으면 Claude Code에게 물어보세요.

```
"이 함수를 어느 파일에 넣어야 하네스와 맞아?"
```

---

**환경변수를 코드에 직접 써넣기**

AWS 키, DB 비밀번호 같은 민감한 정보를 코드 안에 직접 쓰면 안 됩니다. Bitbucket에 push하는 순간 누구나 볼 수 있게 됩니다.

하네스는 이런 값을 **환경변수 파일(.env)** 또는 **AWS Secrets Manager**로 관리합니다. 하네스 구조 안에서 작업하면 Claude Code가 자동으로 올바른 방식을 사용합니다.

코드에서 비밀번호처럼 보이는 문자열이 직접 보인다면, 멈추고 강사에게 물어보세요.
