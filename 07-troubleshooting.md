# 07 · 막혔을 때 / 자주 나는 에러

`[참조]` — 막혔을 때 찾아보는 페이지입니다. 처음부터 읽지 않아도 됩니다.

> 흔한 에러와 복구법, 되돌리기, 훅 ✅이 안 뜰 때 체크리스트.

---

> 📎 **회사 노션 상세 자료** — 이 장의 심화 내용과 보충 설명은 노션에 있습니다.  
> 링크: <!-- NOTION_LINK -->

---

## git 관련

### "Your branch is behind 'origin/main'"

다른 사람이 main에 merge한 뒤 내 로컬이 뒤처진 상태입니다. 내 브랜치에 최신 main을 당겨와야 합니다.

```bash
git checkout main
git pull
git checkout 내-브랜치
git merge main
```

충돌이 나면 아래 "merge conflict" 항목을 확인하세요.

---

### "merge conflict" — 충돌 해결

같은 파일의 같은 줄을 두 사람이 다르게 수정했을 때 생깁니다. git이 어느 쪽을 선택할지 모르기 때문에 사람이 직접 결정해야 합니다.

**1. 충돌 파일 찾기**

```bash
git status
```

`both modified:` 라고 표시된 파일이 충돌 파일입니다.

**2. 파일 열어 충돌 구간 찾기**

파일 안에 이런 표시가 보입니다.

```
<<<<<<< HEAD
내가 수정한 내용
=======
다른 사람이 수정한 내용
>>>>>>> main
```

**3. 원하는 내용만 남기고 표시 삭제**

`<<<<<<<`, `=======`, `>>>>>>>` 줄을 모두 지우고, 최종적으로 남길 내용만 남깁니다.

**4. 저장 후 commit**

```bash
git add 파일이름
git commit -m "resolve merge conflict"
```

---

### 마지막 commit 되돌리기

상황에 따라 두 가지 방법이 있습니다.

| 방법 | 언제 쓰나 | 안전도 |
| --- | --- | --- |
| `git revert HEAD` | push한 뒤, 이미 공유된 경우 | ✅ 안전. 되돌리는 commit을 새로 만듦 |
| `git reset --hard HEAD~1` | push 전, 내 로컬에만 있는 경우 | ⚠️ 위험. commit 기록이 삭제됨 |

push한 뒤에는 무조건 `git revert`를 씁니다. `reset --hard`는 공유된 이력을 지워서 팀원에게 피해를 줄 수 있습니다.

```bash
git revert HEAD
```

---

### 잘못된 브랜치에서 작업했을 때

main에서 직접 작업해버린 경우, 아직 commit 전이라면 변경 내용을 임시 저장하고 브랜치를 옮길 수 있습니다.

```bash
git stash           # 변경 내용 임시 저장
git checkout -b 새-브랜치이름
git stash pop       # 임시 저장한 내용 가져오기
```

이미 commit까지 한 경우에는 강사에게 문의하세요.

---

## 배포 관련

### 훅 ✅이 안 뜰 때 체크리스트

순서대로 확인합니다.

- [ ] Bitbucket에서 PR이 main으로 merge됐는가?
- [ ] Amplify 콘솔에서 빌드가 진행 중이거나 완료됐는가?
- [ ] 빌드 로그에 빨간 오류가 있는가? → 오류 메시지를 복사해 Claude Code에 붙여넣기
- [ ] 백엔드 Lambda 배포도 완료됐는가?
- [ ] 훅 스크립트를 수동으로 실행해봤는가?

빌드 로그는 AWS Amplify 콘솔 → 해당 앱 → 최근 빌드에서 확인할 수 있습니다.

---

### "빌드는 됐는데 사이트가 안 열림"

빌드 성공과 사이트 접속은 다릅니다.

- **CloudFront 캐시** 문제일 수 있습니다. 브라우저에서 강력 새로고침(`Ctrl+Shift+R` / `Cmd+Shift+R`)을 해보세요.
- **SSO 세션**이 만료됐을 수 있습니다. 회사 계정으로 다시 로그인하세요.
- URL이 바뀌었을 수 있습니다. Amplify 콘솔에서 최신 URL을 확인하세요.

---

### AWS 자격증명 오류

터미널에서 이런 오류가 나면:

```
An error occurred (ExpiredTokenException): ...
```

또는

```
Unable to locate credentials
```

AWS 자격증명이 만료되거나 없는 상태입니다.

```bash
aws sso login
```

을 실행하고 브라우저에서 회사 계정으로 로그인합니다. 완료되면 다시 명령어를 실행하세요.

---

## 터미널 관련

### "command not found" — 명령어가 없다고 할 때

```
bash: aws: command not found
```

해당 도구가 설치되지 않았거나 경로가 잡히지 않은 상태입니다.

- AWS CLI라면: [setup-windows](setup-windows.md) 4단계를 다시 확인하세요 (Mac도 동일 설치 방법).
- git이라면: `git --version`으로 설치 여부 먼저 확인하세요.

---

### "Permission denied" — 권한이 없다고 할 때

```
Permission denied (publickey)
```

Bitbucket SSH 키 설정이 안 된 경우입니다. HTTPS 방식으로 clone했다면 이 오류는 잘 안 납니다. SSH 방식 사용 중이라면 강사에게 문의하세요.

---

### 프로세스가 안 멈출 때

| 키 | 동작 |
| --- | --- |
| `Ctrl + C` | 실행 중인 프로세스 즉시 종료. 대부분 이걸로 해결 |
| `Ctrl + Z` | 프로세스를 백그라운드로 보냄. 종료가 아님 |

`Ctrl+Z`를 눌렀는데 프롬프트가 돌아온 것 같아도 프로세스는 살아있습니다. 완전히 끝내려면 `Ctrl+C`를 쓰거나, 백그라운드 프로세스를 종료하려면 `kill %1`을 입력하세요.

---

### 경로를 잃어버렸을 때

지금 어디 있는지 모르겠으면:

```bash
pwd
```

홈으로 돌아가려면:

```bash
cd ~
```

작업하던 레포 폴더로 가려면:

```bash
cd ~/작업폴더이름
```

---

## 그래도 모르겠으면

**Claude Code에 에러 메시지 전체를 붙여넣기**

에러가 나면 터미널 화면을 그대로 복사해서 Claude Code에 붙여넣습니다. "이 에러가 뭔지 모르겠어, 해결해줘"라고만 해도 됩니다. 에러 메시지를 요약하거나 직접 해석하려 하지 마세요. 전체를 그대로 줘야 Claude Code가 정확히 파악합니다.

---

**강사에게 화면 공유로 질문하기**

말로 설명이 어려우면 화면을 공유하세요. 터미널 화면과 에러 메시지가 보이면 대부분 빠르게 해결됩니다.

질문할 때 아래를 같이 알려주면 더 빠릅니다.

- 무엇을 하려다가 막혔는지
- 어떤 명령어를 입력했는지
- 어떤 메시지가 나왔는지
