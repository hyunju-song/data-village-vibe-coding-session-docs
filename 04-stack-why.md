# 04 · 왜 이 스택이 표준인가

`[읽기]` — 개념만 읽고 넘기면 됩니다. 외울 필요 없습니다.

> Next.js · Flask · DynamoDB/Athena/S3를 왜 회사 표준으로 골랐는지, 각 줄이 무슨 일을 하는지 한 줄씩.

---

## 개념

03장에서 대시보드가 세 조각(프론트 / 백 / DB)으로 나뉜다는 걸 봤습니다. 이 장에서는 각 조각에 **어떤 도구를 쓰는지**, 그리고 **왜 그 도구인지**를 설명합니다.

```
프론트엔드       백엔드         데이터
─────────        ──────         ──────────────────
Next.js    ◄──► Flask     ◄──► DynamoDB   (빠른 조회)
                               Athena + S3 (대용량 분석)
```

---

### Next.js — 프론트엔드

**화면을 빠르게 만들 수 있는 틀**입니다. React 기반으로 동작합니다.

버튼·그래프·표 같은 UI 요소를 컴포넌트 단위로 조립합니다. 배포하면 사용자 브라우저에서 실행됩니다.

> 데이터 분석가가 React를 처음부터 배울 필요는 없습니다. 하네스가 틀을 잡아주고, Claude Code가 코드를 만들어줍니다. 우리는 "어떤 화면을 원하는지"를 프롬프트로 말하면 됩니다.

---

### Flask — 백엔드

**파이썬으로 API를 만드는 가벼운 프레임워크**입니다.

데이터 분석가에게 친숙한 파이썬을 그대로 씁니다. 프론트엔드의 요청을 받아 DB에서 데이터를 꺼내고, 필요하면 계산·가공을 거쳐 결과를 돌려줍니다.

Django 같은 더 무거운 프레임워크도 있지만, 대시보드 수준에서는 Flask로 충분합니다.

---

### DynamoDB — 빠른 조회용 DB

**AWS의 키-값 저장소**입니다. "이 사용자의 설정값", "이 버튼의 최근 클릭 수" 같이 **즉시 응답이 필요한 작은 데이터**에 적합합니다.

응답이 매우 빠르고, 서버 관리가 필요 없습니다(AWS가 알아서 합니다).

---

### S3 + Athena — 대용량 분석용

**S3**는 AWS의 파일 창고입니다. CSV, Parquet 같은 대용량 데이터 파일을 저장합니다.

**Athena**는 S3에 쌓인 파일을 **SQL로 바로 조회**하는 도구입니다. 서버를 따로 띄우지 않아도 되고, 쿼리를 실행한 만큼만 비용이 청구됩니다.

데이터 분석가에게 친숙한 SQL을 그대로 사용하기 때문에 진입 장벽이 낮습니다.

---

### 왜 이 조합인가

| 이유 | 설명 |
| --- | --- |
| 파이썬 친숙도 | 데이터 분석가는 이미 파이썬을 씁니다. 백엔드(Flask)와 분석(Athena SQL)이 기존 언어 그대로입니다. |
| AWS 회사 표준 | 인프라가 이미 AWS 위에 있습니다. 새 도구를 도입할 필요가 없습니다. |
| 하네스와의 궁합 | 이 세 도구로 이루어진 구조를 하네스가 이미 알고 있습니다. 틀에서 벗어나지 않으면 배포·보안·모니터링이 자동으로 따라옵니다. |
| 관리 부담 최소화 | DynamoDB·Athena·S3는 모두 서버리스입니다. 서버를 직접 관리하지 않아도 됩니다. |

---

## 쉬운 비유

요리 도구에 비유하면 이렇습니다.

| 도구 | 비유 |
| --- | --- |
| Next.js | 플레이팅 접시. 손님 눈에 보이는 최종 모양 |
| Flask | 냄비. 재료를 받아 요리(가공)하는 도구 |
| DynamoDB | 조리대 위 작은 그릇. 바로 꺼내 쓸 재료 |
| S3 | 대형 냉동 창고. 대량 재료 보관 |
| Athena | 창고 검색 시스템. "지난달 치킨 재료 얼마나 쓰셨어요?" 질문에 바로 답 |

---

## 해보기

이 장은 읽기 장입니다. 직접 실습은 없습니다.

아래 질문에 답해 보세요.

- 사용자가 대시보드에서 날짜를 바꾸면, 그 날짜 조건으로 데이터를 조회하는 건 어느 도구인가요?
- 수백만 행짜리 CSV 파일을 SQL로 조회하고 싶으면 어느 도구를 쓰나요?
- 파이썬 pandas로 하던 분석을 백엔드에서 그대로 쓸 수 있는 이유는 뭔가요?

---

## 흔한 실수

**"Flask 말고 다른 걸 쓰면 안 되나요?"**

기술적으로는 가능합니다. 하지만 하네스는 이 스택을 기준으로 만들어졌습니다. 다른 도구를 끼워 넣으면 하네스의 자동화 혜택(배포, 보안, 모니터링)이 깨집니다. 혼자 모든 설정을 다시 해야 합니다.

표준을 지키는 이유는 "이게 최고라서"가 아니라 **"팀 전체가 같은 틀 위에 있어야 서로 도울 수 있어서"** 입니다.

---

**"이 도구들을 다 외워야 하나요?"**

아닙니다. 이 장의 목적은 지도를 머릿속에 넣는 것입니다. "프론트는 Next.js, 백은 Flask, 데이터는 AWS"라는 큰 그림만 기억하면 됩니다. 세부 사용법은 실습하면서 자연스럽게 익혀집니다.
