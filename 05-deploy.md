# 05 · 배포란 무엇인가

`[읽기]` — 개념만 읽고 넘기면 됩니다. 외울 필요 없습니다.

> "내 결과물에 URL이 생겨 남이 본다"는 개념과 프론트·백이 어디로 배포되는지(+SSO). 설정은 하네스가 처리.

---

> 📎 **회사 노션 상세 자료** — 이 장의 심화 내용과 보충 설명은 노션에 있습니다.  
> 링크: <!-- NOTION_LINK -->

---

## 개념

### 배포란

코드를 내 컴퓨터에서만 실행하면 나만 볼 수 있습니다. **배포**는 그 코드를 인터넷 서버에 올려서 **URL이 생기고 남이 볼 수 있게 만드는 것**입니다.

로컬(내 컴퓨터)에서 잘 되는 것과, 배포해서 남이 볼 수 있는 것은 다릅니다. 배포 전까지는 "내 방에서만 열리는 문서"입니다.

---

### 프론트엔드는 어디로 배포되나

Next.js로 만든 화면은 **CloudFront + S3** 위에 올라갑니다. 정적 파일로 빌드한 결과물을 S3 버킷에 업로드하고, CloudFront가 그 파일을 전 세계 엣지 서버에서 빠르게 제공합니다.

main에 merge하면 하네스가 자동으로 빌드 → S3 업로드 → CloudFront 캐시 갱신까지 처리합니다. 직접 배포 명령을 입력할 필요가 없습니다.

---

### 백엔드는 어디로 배포되나

Flask로 만든 API는 **AWS Lambda** 또는 **ECS** 위에서 실행됩니다.

- **Lambda** — 요청이 들어올 때만 실행되고 끝납니다. 서버를 항상 켜둘 필요가 없어 비용이 적습니다.
- **ECS** — 컨테이너를 계속 켜두는 방식입니다. 요청이 많거나 응답 시간이 중요할 때 씁니다.

어느 쪽을 쓸지는 하네스가 결정합니다. 우리는 코드만 작성하면 됩니다.

---

### SSO — 회사 계정으로만 접속

배포된 대시보드에 아무나 들어오면 안 됩니다. **SSO(Single Sign-On)** 는 회사 계정으로 로그인한 사람만 접속할 수 있게 막아주는 장치입니다.

실제로 URL을 열면 회사 로그인 페이지로 먼저 이동하고, 인증이 되면 대시보드가 열립니다.

SSO 설정은 하네스가 처리합니다. 우리가 직접 설정할 필요 없습니다.

---

### 훅 ✅ — 배포 성공 신호

배포가 잘 됐는지 확인하는 방법이 **훅(hook) ✅** 입니다.

> ℹ️ 이 훅은 **실습·부트캠프 레포(`practice`, `bootcamp`)에서 별도로 제공하는 검증 스크립트**입니다. 하네스 플러그인(`fe-agent-harness`) 자체 기능이 아닙니다.

배포 후 스크립트가 자동으로 프론트엔드 URL이 열리는지, 백엔드 API가 응답하는지 등을 점검하고 ✅ 체크가 붙으면 배포 성공입니다.

훅이 ❌ 이거나 아예 안 뜨면 배포가 실패한 것입니다. [07 · 막혔을 때](07-troubleshooting.md)를 확인하세요.

---

### 전체 흐름 한눈에

```
내 컴퓨터에서 코드 작성
    │
    ▼
Bitbucket에 push → PR → main merge
    │
    ▼ (하네스가 자동 감지)
프론트 빌드 → S3 업로드 → CloudFront ► URL 생성
백엔드 배포 (Lambda/ECS)                  │
    │                                     ▼
    ▼                              SSO 인증 후 접속 가능
훅 ✅ 확인
```

---

## 쉬운 비유

| 상황 | 비유 |
| --- | --- |
| 로컬에서 실행 | 내 방에서 혼자 읽는 문서 |
| 배포 | 사내 공용 게시판에 붙이기 |
| URL | 게시판에서 내 문서의 위치 |
| SSO | 게시판이 있는 사무실 출입 카드 |
| 훅 ✅ | 게시판에 잘 붙었는지 담당자가 확인 도장 찍기 |

---

## 해보기

이 장은 읽기 장입니다. 실제 배포는 **실습 02**에서 직접 해봅니다.

아래 질문에 답해 보세요.

- main에 merge만 하면 배포가 자동으로 되는 이유는 뭔가요?
- SSO가 없으면 어떤 일이 생길 수 있나요?
- 훅 ✅이 안 뜨면 가장 먼저 뭘 확인해야 하나요?

---

## 흔한 실수

**"로컬에서는 잘 됐는데 배포하면 안 돼요"**

가장 흔한 상황입니다. 원인은 대부분 **환경 차이**입니다.

내 컴퓨터에는 설치된 라이브러리나 환경변수가 서버에는 없을 수 있습니다. 또는 내 컴퓨터에서만 쓰던 파일 경로가 서버에서는 다를 수 있습니다.

하네스는 이 차이를 최소화하도록 설계됐지만, 하네스 밖에서 임의로 설정을 바꾸면 이런 문제가 생깁니다.

---

**"URL을 팀원에게 공유했는데 못 들어간다고 해요"**

SSO 때문입니다. 접속하려는 사람이 회사 계정으로 인증이 된 상태여야 합니다. 외부 계정이나 로그아웃 상태에서는 접속할 수 없습니다.

회사 이메일로 로그인한 뒤 다시 URL을 열어보라고 안내하세요.
