# 03 · 대시보드는 세 조각

`[읽기]` — 개념만 읽고 넘기면 됩니다. 외울 필요 없습니다.

> 내가 만드는 대시보드가 화면(프론트) · 부엌(백) · 창고(DB)로 나뉜다는 지도를 그립니다.

---

> 📎 **회사 노션 상세 자료** — 이 장의 심화 내용과 보충 설명은 노션에 있습니다.  
> 링크: <!-- NOTION_LINK -->

---

## 개념

대시보드를 만들 때 코드를 한 덩어리로 짜는 게 아닙니다. 역할이 다른 **세 조각**이 서로 대화하며 하나의 화면을 만듭니다.

---

### 프론트엔드 — 사용자가 보는 화면

브라우저에서 실행되는 부분입니다. 버튼, 그래프, 텍스트처럼 **눈에 보이는 모든 것**이 여기에 해당합니다.

사용자가 버튼을 클릭하거나 필터를 바꾸면, 프론트엔드가 백엔드에 "이 데이터 줘"라고 요청을 보냅니다.

---

### 백엔드 — 요청을 처리하는 부엌

사용자 눈에는 보이지 않지만, 프론트엔드의 요청을 받아 **데이터를 가져오거나 계산**합니다.

DB에서 데이터를 꺼내 정리한 뒤 프론트엔드에 돌려줍니다. 보안 검사, 권한 확인도 여기서 합니다.

---

### 데이터베이스(DB) — 데이터를 보관하는 창고

숫자·텍스트·파일이 실제로 저장되는 곳입니다. 백엔드만 DB에 직접 접근합니다. 프론트엔드는 DB를 직접 건드리지 않습니다.

---

### 세 조각이 대화하는 흐름

```
사용자 클릭
    │
    ▼
프론트엔드 ──[요청]──► 백엔드 ──[조회]──► DB
                                          │
프론트엔드 ◄──[화면 업데이트]── 백엔드 ◄──[데이터]
```

1. 사용자가 화면에서 무언가를 합니다 (클릭, 필터 변경 등).
2. 프론트엔드가 백엔드에 요청을 보냅니다.
3. 백엔드가 DB에서 필요한 데이터를 가져옵니다.
4. 백엔드가 데이터를 정리해서 프론트엔드에 돌려줍니다.
5. 프론트엔드가 화면을 새로 그립니다.

이 흐름은 어떤 대시보드를 만들든 거의 동일합니다.

---

## 쉬운 비유

식당으로 생각하면 쉽습니다.

| 식당 | 대시보드 |
| --- | --- |
| 홀 (손님이 앉는 공간) | 프론트엔드 |
| 주방 (요리하는 곳) | 백엔드 |
| 냉장고·창고 (재료 보관) | 데이터베이스 |

손님(사용자)은 홀(프론트엔드)에서 주문합니다. 홀 직원이 주방(백엔드)에 주문을 전달합니다. 주방은 창고(DB)에서 재료를 꺼내 요리한 뒤 홀로 내보냅니다.

손님이 냉장고를 직접 열지는 않습니다. 항상 주방을 통해야 합니다.

---

## 해보기

이 장은 읽기 장입니다. 직접 실습은 없습니다.

대신 아래 질문에 답해 보세요. 읽은 내용이 머릿속에 남아 있는지 확인합니다.

- 내가 대시보드에서 날짜 필터를 바꾸면 어느 조각이 먼저 움직이나요?
- DB에 저장된 데이터를 꺼내오는 건 어느 조각의 역할인가요?
- 프론트엔드가 DB에 직접 접근하지 않는 이유는 뭔가요? (아래 흔한 실수 참고)

---

## 흔한 실수

**"프론트엔드가 DB에 직접 접근하면 안 되나요?"**

안 됩니다. 이유는 두 가지입니다.

첫째, **보안**입니다. 프론트엔드 코드는 브라우저에서 실행되기 때문에 누구나 볼 수 있습니다. DB 접속 정보(주소, 비밀번호)를 프론트엔드에 넣으면 그대로 노출됩니다.

둘째, **제어**입니다. 백엔드를 통하면 "이 사람은 이 데이터만 볼 수 있어"처럼 권한을 걸 수 있습니다. 직접 접근하면 제어할 방법이 없습니다.

백엔드는 그래서 존재합니다. 문지기 역할입니다.
